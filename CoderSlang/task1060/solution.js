/**
 * Implement a function max that takes three numbers and returns the maximum
 *
 * */

import { max } from './helper.js';

const a = 1;
const b = 2;
const c = 3;

console.log(`The max is ${max(a, b, c)}`);